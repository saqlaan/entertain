import styled from 'styled-components/native'

export const StyledSpacer = styled.View`
  margin-horizontal: ${({ marginHorizontal }) => marginHorizontal}px;
  margin-vertical: ${({ marginVertical }) => marginVertical}px;
`
